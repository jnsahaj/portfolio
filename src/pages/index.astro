---
import Layout from "../layouts/Layout.astro";
import { Icon } from "astro-icon/components";

const links = [
    { label: "Github", href: "https://github.com/jnsahaj" },
    { label: "LinkedIn", href: "https://www.linkedin.com/in/sahaj-jain-9099641ab/" },
    { label: "Email", href: "mailto://sahajjain13+business@gmail.com" },
];
---

<Layout>
    <main class="mt-16 text-sm text-gray-300">
        <h2 class="text-3xl text-white font-bold">Sahaj Jain</h2>
        <section class="mt-8">
            <p>Hey, I’m Sahaj—a software engineer building refined experiences on the web.</p>
            <br />
        </section>

        <section class="text-xs text-gray-400">
            <p class="flex mb-1 gap-2 items-center italic">
                <Icon name="octicon:location-16" />
                Haryana, India
            </p>
            <p class="flex mb-1 gap-2 items-center italic">
                <Icon name="octicon:briefcase-16" />
                SDE at<a
                    class="underline underline-offset-1 decoration-gray-600 hover:decoration-current transition"
                    href="https://myhq.in">myHQ</a
                >
            </p>
            <p class="flex mb-1 gap-2 items-center italic">
                <Icon name="octicon:book-16" />
                B.E. (Hons.) from BITS Pilani, Goa Campus
            </p>
        </section>
        <section>
            <br />
            <p>
                I am interested in all things software, because it is so accessible. I primarily work on the web, and
                occassionaly some low-level systems stuff. I use neovim <i>(btw)</i>
            </p>
        </section>

        <h5 class="text-gray-500 text-xs mt-16">Featured Projects</h5>
        <section class="mt-6 grid grid-cols-1 md:grid-cols-2 gap-2 gap-x-10">
            <a href="https://github.com/jnsahaj/monkey-rs" class="flex flex-col gap-2 b py-4 rounded group">
                <div
                    class="text-lg flex items-center gap-1 underline group-hover:decoration-current italic underline-offset-2 decoration-gray-600 transition"
                >
                    monkey-rs
                </div>
                <p class="text-xs text-gray-500">
                    An interpreter and [WIP] compiler for a fully-featured fictional language, written in Rust
                </p>
                <p class="text-xs text-gray-500">Inspired by "Writing an Interpreter in Go"</p>
            </a>
            <a href="https://altr.iamsahaj.xyz" class="md:row-span-2 flex flex-col gap-2 group py-4 rounded">
                <div
                    class="text-lg flex items-center gap-1 underline group-hover:decoration-current italic underline-offset-2 decoration-gray-600 transition"
                >
                    altr
                </div>
                <p class="text-xs text-gray-500">
                    Smart refactoring tool that helps you effortlessly rename variables, functions, and other code
                    elements while maintaining your preferred casing conventions
                </p>
                <p class="text-xs text-gray-500">
                    Uses WASM to interface the UI written in React with the Rust layer, which performs the actual
                    operations.
                </p>
                <p class="text-xs text-gray-500">
                    This tool is also available as a CLI tool and as a standalone Rust crate
                </p>
            </a>
            <a href="https://github.com/jnsahaj/rate-limiter-lab" class="flex flex-col gap-2 group py-4 rounded">
                <div
                    class="text-lg flex items-center gap-1 underline group-hover:decoration-current italic underline-offset-2 decoration-gray-600 transition"
                >
                    rate-limiter-lab
                </div>
                <p class="text-xs text-gray-500">An interactive demo to analyse different rate limiter algorithms</p>
                <p class="text-xs text-gray-500">
                    Uses docker to setup Redis that serves as the in-memory DB to check if a request should be blocked
                </p>
            </a>
        </section>
        <section class="flex text-xs gap-6 justify-center mt-24 text-gray-100 decoration-gray-400 font-thin">
            {
                links.map((link) => (
                    <a class="hover:underline underline-offset-4" href={link.href}>
                        {link.label}
                    </a>
                ))
            }
        </section>
    </main>
</Layout>
